name: G lister chemins
on:
  workflow_dispatch:

jobs:
  lister:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Chemins publies par StockAnalysis
        run: python src/univers_intl.py --lister
